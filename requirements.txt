# Core dependencies
torch>=2.0
numpy>=1.26
joblib>=1.3

# Optimization solver (a valid Gurobi license is required)
gurobipy>=11.0

# Predict-then-optimize toolbox (provides knapsackModel, portfolioModel,
# implicitMLE and SPOPlus differentiation methods)
pyepo>=0.3.7

# Optional: only needed for the GPU 1D-knapsack solver
# (knapsack/solver.py:solver_X_1D_knapsack_GPU)
numba>=0.59

# Optional: experiment tracking (used in offline mode by run_experiments.py)
wandb>=0.16

# Optional: only needed by knapsack/datasets/trainset_gap/display_gap.py
matplotlib>=3.8
